// Paste your Supabase project values here (Supabase → Project Settings → API).
// The anon key is designed to be public — staff can only do what the PIN-checked
// database functions allow. Never put the service_role key here.
window.BHL_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY",
  // Second clock shown next to the working clock (the team's local time).
  LOCAL_TZ: "Asia/Manila",
  LOCAL_LABEL: "Manila",
};
